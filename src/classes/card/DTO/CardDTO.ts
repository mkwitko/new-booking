export type CardListType = {
  brand: string;
  cardNumber: string;
  entity: string;
  lastModified: string;
  tokenized: string;
  typecard: string;
};

export type VcnList = {
  rcnToken: string;
  typecard: string;
  entity: string;
};
